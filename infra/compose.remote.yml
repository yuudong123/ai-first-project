# 원격 데이터 수신·추론 전용. 원본 Compose와 함께 사용한다.
services:
  inference:
    command: [python, -m, src.runtime.multi_inference]
    environment:
      KAFKA_BROKER: "192.168.133.108:9092"
      KAFKA_TOPIC: "hydraulic.sensor.multi.raw"
  api:
    environment:
      HYDROTWIN_REMOTE: "1"
